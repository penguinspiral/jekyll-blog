version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - bundler cache
    build:
      commands:
        - /usr/local/bundle/bin/jekyll build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths:
      - vendor/**/*
